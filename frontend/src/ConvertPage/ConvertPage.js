import * as React from "react";
import {Form, Button, Input, } from 'antd';

const { Search } = Input;

export class ConvertPage extends React.Component {
    render() {
        return(
            <div>
                <p>text2image page</p>
            </div>
        )
    }
}